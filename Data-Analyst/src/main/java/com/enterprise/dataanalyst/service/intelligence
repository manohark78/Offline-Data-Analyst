
You are a senior software architect and Java backend expert.

I am building an offline AI-powered data analyst system using:

- Spring Boot (Java)
- DuckDB (local database)
- A local LLM (for SQL generation only)

IMPORTANT:
This system must work WITHOUT hardcoded rules and WITHOUT relying on fixed column names.
It must work on ANY dataset dynamically.

---

CURRENT PROBLEM:

The system can generate SQL, but it does NOT understand the data.

Example issues:

- User asks: "check payments"
  → system must map "check" to the correct column (even if column name is unknown)

- User asks: "female users"
  → if no gender column exists, system must NOT guess blindly
  → it should decide whether to infer, ask, or refuse

- Column names vary across datasets:
  purchase / payment_mode / txn_type
  → system must NOT depend on column names

---

GOAL:

Build a DATA INTELLIGENCE LAYER that makes the system:

1. Data-aware (understands column values)
2. Dynamic (works with unknown schemas)
3. Non-hardcoded (no fixed keyword mapping)
4. Efficient (no full table scan per query)
5. Offline (no API calls)

---

DESIGN REQUIREMENTS:

Design and generate clean Java (Spring Boot) code for:

---

1) DataProfilerService

Purpose:

- Analyze uploaded table data

Responsibilities:

- Detect column type using uniqueness ratio:
  - CATEGORICAL (few unique values)
  - IDENTIFIER (mostly unique)
  - NUMERIC (numbers)
- Store up to 50 sample values ONLY for categorical columns

---

2) ColumnValueIndexService

Purpose:

- Build fast lookup for value → column mapping

Example:
"check" → purchase column

Requirements:

- Case-insensitive matching
- Efficient in-memory structure
- No database dependency

---

3) ColumnRelevanceService

Purpose:

- Find best column for a query

Logic:

1. Tokenize query
2. Match tokens against column VALUES (highest priority)
3. Fallback to column name similarity

---

4) DecisionEngine

Purpose:

- Decide how to handle query

Output types:

- DIRECT (column exists)
- VALUE_BASED (value match found)
- INFER (derived column needed)
- ASK (ambiguous query)
- REFUSE (data insufficient)

IMPORTANT:

- No hardcoded keywords
- Decision must be generic and data-driven

---

5) Required Models:

- ColumnProfile
- ColumnType (enum)
- ColumnRef
- QueryContext
- DecisionResult

---

CONSTRAINTS:

- DO NOT use LLM for row-by-row processing
- DO NOT scan full table per query
- DO NOT hardcode mappings like "payment → purchase"
- KEEP logic generic and reusable
- Keep classes small and clean

---

INTEGRATION:

Explain where to integrate:

1. DataProfilerService → after data ingestion
2. ColumnValueIndexService → during profiling
3. ColumnRelevanceService → before SQL generation
4. DecisionEngine → before calling SQL LLM

---

OUTPUT FORMAT:

- Provide complete Java classes
- Clean code with comments
- Proper package structure
- Explain flow after code

---

GOAL:

The system should intelligently map user queries to correct data WITHOUT prior knowledge of schema.

---

Now generate the implementation step-by-step starting with DataProfilerService.
